# nixpacks.toml

[phases.build]
# Устанавливаем зависимости для сервера
cmds = ["cd server && npm install"]

[start]
# Запускаем сервер (миграции теперь происходят внутри приложения)
cmd = "cd server && npm start"